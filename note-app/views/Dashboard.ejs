<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard</title>
    <link rel="stylesheet" href="/src/output.css">
</head>

<body class="bg-teal-200 h-screen">
    <p class="text-2xl">hello wolrd is this wroking</p>
    <ul>
        <% users.forEach(user=>{ %>
            <li>
                <h1>
                    <%=user.name%>
                </h1>
                <h2>
                    <%=user.surname%>
                </h2>
                <p>
                    <%=user.id%>
                </p>
            </li>
            <% }) %>
    </ul>

    <ul>
    <% for(let x=1; x<10; x++ ) { %>
        <li>
            <%= x %>
        </li>
    <% } %>
   </ul>
</body>

</html>